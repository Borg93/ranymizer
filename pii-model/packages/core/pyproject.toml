[project]
name = "ranymizer-pii-core"
version = "0.1.0"
description = "Shared kernel for the Ranymizer Swedish-PII workspace: PII label taxonomy + injectable LLM endpoint config."
requires-python = ">=3.11"
dependencies = ["pydantic>=2.0.0"]

[build-system]
requires = ["uv_build>=0.10.11,<0.11.0"]
build-backend = "uv_build"
