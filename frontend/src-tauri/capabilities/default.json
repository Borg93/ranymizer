{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core only — no fs/shell/http/dialog. Add scoped permissions only if a future #[tauri::command] needs them.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
